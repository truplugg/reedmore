# READMORE

Украинский книжный магазин с доставкой по ЕС и Украине.
Статический сайт без сборки: открывается по любому HTTP-адресу, деплоится
куда угодно — GitHub Pages, Netlify, Vercel, S3, nginx.

---

## Быстрый старт

Модули подключены как ES-модули, поэтому нужен HTTP (через `file://`
браузер их заблокирует):

```bash
python3 -m http.server 8080
# открыть http://localhost:8080
```

Любой статический сервер подойдёт: `npx serve`, `php -S`, nginx.

---

## Что внутри

```
index.html                  вся страница
assets/css/
  tokens.css                палитра, шрифты, типографическая шкала, темы
  base.css                  ресет, типографика, утилиты
  layout.css                шапка, навигация, сетка полки, подвал
  components.css            кнопки, чипы, бейджи, поля, тосты
  book.css                  книга как 3D-объём — ключевой компонент
  sections.css              герой, добірки, доставка, журнал, клуб
  overlays.css              корзина, карточка книги, командная палитра
assets/js/
  data/books.js             каталог (26 книг)
  covers.js                 генератор обложек
  book3d.js                 объём книги, поворот и раскрытие
  stitch.js                 мотивы вышивки, посчитанные по стежкам
  catalog.js                поиск, фильтры, сортировка, пагинация
  cart.js                   корзина и список желаемого
  dialog.js                 полная карточка книги
  delivery.js               калькулятор доставки по ЕС
  palette.js                поиск по ⌘K / Ctrl+K
  motion.js                 появление секций, бегущая строка, герой
  store.js                  состояние, localStorage, конвертация валют
  i18n.js                   украинский и английский
  ui.js                     шапка, тема, язык, валюта, тосты
  main.js                   инициализация
```

---

## Книга как объект

Каждая книга на сайте — настоящий трёхмерный объём: передняя сторонка,
блок страниц с видимым обрезом, корешок и задняя сторонка. Компонент
работает в двух режимах.

**`turn` — полка и лента новинок.** Книга стоит развёрнутой на 21°. При
наведении курсора (или первом касании на телефоне) она поворачивается к
читателю, приподнимается и возвращает себе цвет, а все соседние уходят в
чёрно-белое. Повторное действие открывает карточку.

**`open` — герой и карточка книги.** Там, где есть место, обложка
раскрывается на корешке: за ней форзац с вышивкой и первая страница с
аннотацией, издательским знаком и колофоном — формат, бумага, переплёт,
ISBN.

| Устройство | Действие |
|---|---|
| Мышь | наведение поворачивает, клик открывает карточку |
| Палец | первое касание поворачивает, второе открывает карточку |
| Клавиатура | `Tab` поворачивает, `Enter` открывает карточку, `Esc` сбрасывает |

Активна всегда одна книга. `placeSpread()` в `book3d.js` считает сдвиг
раскрытого разворота так, чтобы он не вылез за край контейнера — это
работает и для крайних книг в ряду, и для двух колонок на телефоне.

Хит-зона (`.book__trigger`) намеренно вынесена из 3D-сцены в обёртку
`.book__head`: внутри `transform-style: preserve-3d` порядок элементов
задаёт глубина, а не `z-index`, и обложка перехватывала бы клики.

При `prefers-reduced-motion: reduce` поворот и раскрытие отключаются.

---

## Вышиванка

`assets/js/stitch.js` хранит мотивы, посчитанные по клеткам, как на
настоящей вышивке: восьмиконечная зірка (Берегиня), ромб с крючками,
счётный крест и бегущий зигзаг. Каждый мотив — один `<rect>` на серию
стежков подряд.

Используются в двух местах, и только в них:

- **форзацы** — `endpaperSVG()` в `covers.js` замащивает мотив в цвете
  акцента конкретной книги; виден при раскрытии обложки;
- **орнаментальная линейка** `.ornament` — полоса «зірка · ромб · зірка»
  под героем и по верхнему краю панели клуба.

---

## Данные каталога

`assets/js/data/books.js` — обычный ES-модуль. Заменяется на запрос
к вашему API без изменений в остальном коде: `catalog.js`, `cart.js` и
`palette.js` работают с массивом `BOOKS`.

Поля одной книги:

```js
{
  id, title, titleEn, author, authorEn, year, publisher,
  genre,            // proza | klasyka | poeziia | nonfic | dytiacha
  tags: [],
  format,           // hard | soft
  pages, size, paper, binding, isbn, weight,
  price,            // в гривнах — базовая валюта
  oldPrice,         // если есть скидка
  rating, reviews, stock,
  badge,            // new | best | preorder | last | null
  blurb, blurbEn,   // короткая аннотация — печатается на странице книги
  about, aboutEn,   // развёрнутая — в карточке
  cover: { style, pal, motif },
  endpaper          // star | rhomb | cross | zig
}
```

**Обложки генерируются, а не загружаются.** `covers.js` собирает их из
шести типографических шаблонов (`stack`, `band`, `plate`, `column`,
`serif`, `split`) и девяти геометрических плашек. Ни одна издательская
обложка не воспроизводится. Если появятся права на реальные изображения —
достаточно заменить `coverHTML()` на `<img>`.

---

## Валюты и языки

Валюты объявлены в `store.js` → `CURRENCIES`. Базовая — гривна; курсы
сейчас зашиты константами, перед запуском их нужно брать из фида
платёжного провайдера. Округление задаётся полем `step`.

Языки — в `i18n.js`. Новый язык добавляется одним блоком ключей в `DICT`
и строкой в `LANGS`; разметка уже размечена `data-i18n` и
`data-i18n-attr`, дополнительной работы не требуется.

---

## Доставка

`delivery.js` считает по зонам: базовая ставка плюс цена за килограмм,
вес считается как 380 г на книгу плюс 180 г на коробку. Перевозчик и срок
у каждой зоны свои. Бесплатная доставка — от порога `FREE_FROM_UAH`
в `store.js`. Перед запуском таблицу зон заменить на тарифы перевозчиков.

---

## Состояние

Корзина, список желаемого, язык, валюта, тема и вид каталога хранятся
в `localStorage` под ключом `readmore.v1`. Каждое обращение обёрнуто
в `try/catch`: в приватном окне и при заблокированных данных сайта
хранилище бросает исключение, и страница должна работать всё равно.

Фильтры каталога живут в адресной строке (`?q=&g=&f=&s=&w=`) — отфильтрованную
полку можно отправить ссылкой.

---

## Доступность

Семантическая разметка, видимый фокус, ссылка «к основному содержимому»,
`aria-expanded` на книгах, ловушка фокуса в модальном окне, управление
с клавиатуры во всех оверлеях, уважение к `prefers-reduced-motion`,
поддержка светлой и тёмной темы плюс системной настройки.

---

## Деплой

Обычная статика, шагов сборки нет.

**GitHub Pages:** Settings → Pages → Deploy from a branch → ветка и
корень `/`. Файл `.nojekyll` уже лежит в репозитории.

**Netlify / Vercel:** build command пустой, publish directory — корень.

---

## Что нужно сделать до настоящего запуска

- [ ] Подключить реальный каталог вместо `data/books.js`
- [ ] Курсы валют из фида провайдера вместо констант
- [ ] Тарифы перевозчиков в `delivery.js`
- [ ] Оплата и оформление заказа (кнопка «Оформити замовлення» сейчас заглушка)
- [ ] Отдельные страницы книг вместо модального окна — для поисковой выдачи
- [ ] Подписка на клуб и рассылку на бэкенд

---

## Демоданные

Названия книг, авторы и издательства — настоящие. Цены, наличие, ISBN,
рейтинги и отзывы — тестовые значения. Обложки — оригинальный
типографический дизайн, а не репродукции издательских. Об этом сказано
и в подвале сайта.
